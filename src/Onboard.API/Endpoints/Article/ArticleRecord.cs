using System;
namespace Onboard.API.Endpoints.Article
{
  //public record ArticleRecord(Guid id, string title, string content, string link, Guid userId);
  public record ArticleRecord(Guid id, string title, string content, string link);
}

